# Game Jam 项目协作规则

本项目是一个计划在三天内完成的 Game Jam 项目。所有工作都应遵循以下规则：

- 优先保证项目始终可以运行。
- 统一使用 Godot 4.7.1 和 GDScript，不使用 C#。
- 保持实现简单，避免过度设计。
- 每次只进行小范围、可测试的修改。
- 除非用户明确同意，否则不要添加第三方插件。
- 不要随意修改或删除其他组员的文件。
- 修改前先检查现有项目结构。
- 尽量使用 typed GDScript。
- 变量和函数使用 `snake_case` 命名。
- 不要手动修改 `.godot` 缓存目录。
- 美术资源尚未完成时，可以使用简单的占位图形。
- 完成功能后，说明修改了什么，以及如何在 Godot 中测试。
- 如果无法实际运行 Godot，不要声称已经测试。
- 对尚未确定的玩法，不要自行设计架构、创建场景或实现规则；先向用户确认。
- 即使用户使用英文，也要使用中文回复。
- 每次完成玩法机制的新增、删除或行为修改后，都要同步更新根目录的 `机制.md`。
- `机制.md` 只记录当前实际实现；尚未实现的想法必须明确标记。
- 如果代码与 `机制.md` 不一致，先报告差异，不要自行决定玩法规则。


## 代码维护规则
- 修改现有机制时，优先扩展或替换已有实现，不创建功能重复的平行系统。
- 被新机制取代的旧代码应删除，不保留注释掉的备用实现。
- 每种跨场景状态只能有一个明确的数据来源。
- `Poop` 负责移动、缩回和长度计算。
- `Toilet` 负责本轮流程和玩法系统之间的协调。
- `Economy` 负责金钱和最终价值计算。
- `GameState` 负责天数和跨天堵塞进度。
- `PlayerStats` 是储存量、顺滑度和完整度的唯一数据来源。
- `QTEBar` 负责单次 QTE 的显示和判定，`QTERules` 负责根据属性生成 QTE 配置。
- `StatsHUD` 只负责显示玩家属性，不保存属性副本。
- `PauseMenu` 负责暂停菜单和音量选项，不负责玩法状态。