# Game Jam 项目协作规则

本项目是一个计划在三天内完成的 Game Jam 项目。所有工作都应遵循以下规则：

- 优先保证项目始终可以运行。
- 统一使用 Godot 4.7.1 和 GDScript，不使用 C#。
- 保持实现简单，避免过度设计。
- 每次只进行小范围、可测试的修改。
- 除非用户明确同意，否则不要添加第三方插件。
- 不要随意修改或删除其他组员的文件。
- 修改前先检查现有项目结构。
- 尽量使用 typed GDScript。
- 变量和函数使用 `snake_case` 命名。
- 不要手动修改 `.godot` 缓存目录。
- 美术资源尚未完成时，可以使用简单的占位图形。
- 完成功能后，说明修改了什么，以及如何在 Godot 中测试。
- 如果无法实际运行 Godot，不要声称已经测试。
- 在玩法确定前，不要创建具体的游戏架构、游戏场景或实现玩法。
- 即使用户使用英文，也要使用中文回复。


## 代码维护规则
- 修改现有机制时，优先扩展或替换已有实现，不创建功能重复的平行系统。
- 被新机制取代的旧代码应删除，不保留注释掉的备用实现。
- 每种跨场景状态只能有一个明确的数据来源。
- Poop负责移动和长度，Toilet负责本轮流程，Economy负责金钱，GameState负责天数和跨天进度。
- 新增Manager、Autoload或Resource前，先确认现有结构无法承担该职责。
- 不将当前功能无关的代码一起重构。